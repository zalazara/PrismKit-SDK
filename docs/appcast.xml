<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<!--
  The Sparkle update feed for Prism Inspector.

  Released copies of the app poll this exact URL forever, so it must keep
  working even before the first release exists: an empty channel makes Sparkle
  answer "you are up to date", whereas a 404 makes it show an error dialog.

  The release workflow rewrites this file, signing each entry with the EdDSA
  key. Do not edit it by hand — an unsigned or wrongly signed entry is refused
  by every installed copy.
-->
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>Prism Inspector</title>
        <link>https://zalazara.github.io/PrismKit-SDK/appcast.xml</link>
        <description>Updates for Prism Inspector, the macOS companion for PrismKit.</description>
        <language>en</language>
        <item>
            <title>1.6.0</title>
            <pubDate>Fri, 31 Jul 2026 20:25:43 +0000</pubDate>
            <sparkle:version>81</sparkle:version>
            <sparkle:shortVersionString>1.6.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>New</h3>
<ul>
  <li>When an agent asks where a component lives in the code, it now gets the exact line the modifier was written on instead of a search for the component's name. Components whose name is assembled at runtime, and shared components used on several screens, are found correctly for the first time.</li>
  <li>Selecting a component now shows it drawn on its own next to the same patch of the screen, so a component that is covered, invisible or has no height stops looking like nothing at all. Values an app offers can be changed live from the inspector, and the app re-lays-out for real. The Connection section now says when an app has stopped answering — a breakpoint no longer looks like a screen nobody is touching.</li>
  <li>A new Exploded hierarchy view in the Layers menu stacks a screen's components in depth and lets you orbit around them. Each component is drawn without the ones nested inside it, so a container that only holds other views now looks empty — which is what it actually contributes.</li>
  <li>Prism Inspector can now inspect an app running on a real iPhone or iPad connected to your Mac, not just one in the simulator. Attached devices appear alongside booted simulators in the device menu.</li>
  <li>Values an app offers can now be put back to what they started as — a reset beside each one you have changed, and one that undoes them all.</li>
</ul>
<h3>Fixed</h3>
<ul>
  <li>Devices connected to your Mac now appear by name rather than by serial number.</li>
  <li>When inspecting an app on a physical device, the accessibility hints now point at the device's own Settings rather than at the simulator.</li>
  <li>The exploded hierarchy no longer draws every element twice. The screen behind the components is now the app's own background, with the components lifted off it.</li>
  <li>Typing into a live value no longer fights with the app updating it — what you type is applied when you press Enter or leave the field. Reset is now a labelled button and puts the value back for good instead of flipping between the original and your last edit.</li>
</ul>
<h3>Changed</h3>
<ul>
  <li>Live values, a component drawn on its own, the exploded hierarchy and physical-device support need the inspected app to embed PrismKit 0.3.1 or newer, so update the package in your app to get them. Prism Inspector still connects to apps on older versions; those sections just will not appear.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/zalazara/PrismKit-SDK/releases/download/app-v1.6.0/Prism-Inspector-1.6.0.zip" length="8953593" type="application/octet-stream" sparkle:edSignature="eiY5+ll9ZKRosd5WNVl2cl/t0OAzQcv96jzk/KpC7DtGFKRMgImE9MDdcuQg6diGJFNXh9onWXUVkYYMdMreDQ=="/>
        </item>
        <item>
            <title>1.5.0</title>
            <pubDate>Fri, 31 Jul 2026 02:31:34 +0000</pubDate>
            <sparkle:version>63</sparkle:version>
            <sparkle:shortVersionString>1.5.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>New</h3>
<ul>
  <li>The guideline warnings, the design check findings and the remaining dialogs now follow the language you picked, rather than staying in English — including the rule names, why each rule matters, and the sentence behind every finding.</li>
  <li>Guideline warnings now say when a finding also misses EN 301 549 — the standard behind the European Accessibility Act, which an app sold to consumers in the EU has had to meet since 28 June 2025 — and link to the act. Only contrast counts towards it: tap target size is Apple's ask, not Europe's.</li>
  <li>The App preview chip can now put the simulator into Increase Contrast and into any Dynamic Type size, up to the five accessibility sizes. The app restyles and relayouts live, and the guideline checks re-measure what it drew — so a layout that breaks at larger text shows up here rather than on someone's phone.</li>
</ul>
<h3>Fixed</h3>
<ul>
  <li>Prism Inspector now finds new versions on its own, shortly after launch and hourly while it runs, instead of only when you chose "Check for Updates…". The offer is the same one as before — install now, remind me later, or skip this version.</li>
  <li>Fixed a crash at launch. The check for the disk image the app was installed from ran while the interface was being built and could take the app down with it; it now happens in the background. A failed move to Applications also says what went wrong instead of looking like nothing happened.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/zalazara/PrismKit-SDK/releases/download/app-v1.5.0/Prism-Inspector-1.5.0.zip" length="8556267" type="application/octet-stream" sparkle:edSignature="9ned1k9DIseWn9xMBEn7yAA7vCY7GmTR63DB00WCKexCCwZddeUhEWFzodAhzkLDfQUSfUTkcVRqJuZsf9DdCQ=="/>
        </item>
        <item>
            <title>1.4.0</title>
            <pubDate>Thu, 30 Jul 2026 23:21:51 +0000</pubDate>
            <sparkle:version>57</sparkle:version>
            <sparkle:shortVersionString>1.4.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h3>New</h3>
<ul>
  <li>Prism Inspector now checks the controls on screen against Apple's 44×44 pt minimum tap target. When something is too small a warning count appears in the toolbar, and clicking a warning marks the control on the canvas.</li>
  <li>Prism Inspector now also checks text contrast, reading the colours from the screen as drawn rather than from any theme file. Text that falls short of Apple's 4.5:1 shows up in the same warnings list as undersized controls, with what passes only as large text kept separate from what fails outright.</li>
  <li>Guideline warnings now name the guideline as Apple's, explain what goes wrong for someone using the app, and link to Apple's own page for the rule.</li>
  <li>Guideline warnings are now grouped by rule, so the explanation and the link to Apple's page appear once per rule rather than once per element, and the count beside each rule shows how much of the screen it affects.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/zalazara/PrismKit-SDK/releases/download/app-v1.4.0/Prism-Inspector-1.4.0.zip" length="8442593" type="application/octet-stream" sparkle:edSignature="v9R6Y2EvfSQmEcq0jNCZDgTthFx437kkqaf3bEI15Hh3Yh8+ZVOG2DVbp8QrHNG06jCcvdTctVoYuh88LLF6BA=="/>
        </item>
    </channel>
</rss>